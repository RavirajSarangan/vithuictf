"use client";

import { AuthLoginShell } from "@/components/auth/auth-login-shell";

export default function AdminLoginPage() {
  return (
    <main>
      <AuthLoginShell variant="admin" />
    </main>
  );
}
