"use client";

import { AuthLoginShell } from "@/components/auth/auth-login-shell";

export default function SocialTrackingLoginPage() {
  return (
    <main>
      <AuthLoginShell variant="socialTracking" />
    </main>
  );
}
